import React from 'react'
import { IBoxingMatch } from '../Interfaces/IBoxingMatch'

interface Props {
  boxingMatch: IBoxingMatch
}


export default function BoxingMatchCard(props: Props) {
  return (
    <div>Boxing Match Card</div>
  )
}
